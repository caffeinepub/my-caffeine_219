{
  "frontend/src/index.css": "Global styles with traditional Chinese manuscript OKLCH color palette (parchment, ink, seal-red, gold), Google Fonts import for Noto Serif SC/Sans SC, and utility classes for manuscript borders, seal stamps, and decorative elements.",
  "frontend/tailwind.config.js": "Extended Tailwind config with custom font families (Noto Serif SC, Noto Sans SC), custom colors (seal, gold, parchment, ink), and manuscript/seal box shadows.",
  "frontend/src/hooks/useQueries.ts": "Complete React Query hooks file covering all backend operations (manuscripts, family history, people, relationships, posts, replies, activities, clan contacts, help requests, donation channels) with proper async/await, `#ok`/`#err` Candid variant checking via `__kind__`, error throwing on `#err`, and `queryClient.invalidateQueries` cache invalidation on every mutation success.",
  "frontend/src/App.tsx": "Root app component with session-based passcode gate, section state management extended to five sections (manuscripts/history/people/interaction/contact), rendering ContactClan for the new 'contact' section alongside the existing four sections.",
  "frontend/src/components/PasscodeGate.tsx": "Full-screen passcode entry gate with traditional Chinese heritage theme, shake animation on wrong input, and session persistence on success.",
  "frontend/src/components/Layout.tsx": "Application shell with hero banner, five-item navigation (manuscripts/history/people/interaction/contact with Phone icon for the new 联系家族 item), and footer with caffeine.ai attribution.",
  "frontend/src/components/AncientManuscripts.tsx": "Ancient manuscripts section with file upload and delete; both UploadPasscodeModal and DeletePasscodeModal now use the correct `open` prop instead of `isOpen`.",
  "frontend/src/components/FamilyHistory.tsx": "Family history section with read-only display and inline edit mode; UploadPasscodeModal now uses the correct `open` prop instead of `isOpen`.",
  "frontend/src/components/PeopleDirectory.tsx": "Searchable people directory grid with add/edit/view/delete navigation; fixed to pass the full `person` object (not `personId`) to PersonProfile, resolving the TypeScript prop mismatch.",
  "frontend/src/components/PersonProfile.tsx": "Full person profile view with biography, media files, and relationships tabs; all modal usages now correctly use the `open` prop (not `isOpen`) to match UploadPasscodeModal and DeletePasscodeModal interfaces.",
  "frontend/src/components/PersonForm.tsx": "Add/edit person form with all fields and photo upload; the UploadPasscodeModal now uses the correct `open` prop instead of `isOpen`.",
  "frontend/src/components/UploadPasscodeModal.tsx": "Reusable Dialog modal that prompts for the upload passcode ('dsjzscxx'); the '确认' button text is now black (text-black), calls `onConfirmed` immediately after correct passcode entry, resets state on close, and stops event propagation to prevent interference with outer forms.",
  "frontend/src/components/DeletePasscodeModal.tsx": "Reusable Dialog modal that prompts for the delete passcode ('dsjzyyxw3344') before any deletion; the '确认删除' button text is now black (text-black), shows an error on wrong input and calls onConfirmed only when the passcode is correct.",
  "frontend/src/hooks/useActor.ts": "Initializes the backend actor using the current Internet Identity; creates an anonymous actor when unauthenticated and an identity-authenticated actor when logged in, without any admin token extraction or initialization.",
  "frontend/src/components/ClanInteraction.tsx": "Clan message board component with post listing (newest-first), expandable reply threads, new post/reply forms gated by UploadPasscodeModal, and delete operations gated by DeletePasscodeModal, styled with the traditional Chinese heritage theme.",
  "frontend/src/components/FamilyActivities.tsx": "Family activities component with a list/detail view pattern; the list shows activity cards sorted by date, the detail view shows full description and an image gallery with upload/delete support, all CRUD operations gated by UploadPasscodeModal or DeletePasscodeModal.",
  "frontend/src/components/ContactClan.tsx": "\"联系家族\" section with four sub-sections (leader contacts, group contacts, help requests with admin notes, donation channels); all six passcode modal usages now correctly use the `open` prop instead of `isOpen`."
}